{/* <form onSubmit={(e) => {
  e.preventDefault();
  setAddCard(false);
}
} className={style.option_form}>
  <select className={style.select} onChange={(e) => changeStatus(e, 'inProgress')}>
    <option className={style.option} selected>Select task</option>
    {allTasks.filter(task => task.status === "backlog").map((task) => { return <option className={style.option} key={task.id} value={task.id} >{task.title} </option> })}
  </select>
</form> */}